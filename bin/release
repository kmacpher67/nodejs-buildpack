#!/usr/bin/env bash
# bin/release <build-dir>
# "running bin/release <build-dir>= $1"
cat << EOF
addons: []
default_process_types:
  console: "ls -ltr; pwd; . ~/app/.profile.d/000_apt.sh; Xvfb"
  web: "pwd; . ~/app/.profile.d/000_apt.sh; xvfb-run -e /dev/stdout npm start"
EOF
