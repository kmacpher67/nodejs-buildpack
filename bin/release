#!/usr/bin/env bash
# bin/release <build-dir>
# "running bin/release <build-dir>= $1"
cat << EOF
addons: []
default_process_types:
  web: "pwd; ls -ltra; export DISPLAY=:99; . ~/.profile.d/000_apt.sh; xvfb-run --auto-servernum --server-args='-screen 0 1280x1024x16' -e /dev/stdout npm run startSimple"
EOF
